# Roadmap: Graph-RAG на Lightrag (Production)

**Created:** 2026-03-04
**Goal:** Доставить v1 Graph-RAG пайплайн с ingestion, графовой индексацией, гибридным поиском, наблюдаемостью, безопасностью и UI.

## Phases

### Phase 1 — Ingestion, Versioning & CI/CD Foundation
**Scope:** ING-01, ING-02, ING-03, VER-01, VER-02, VER-03, CICD-01, CICD-02
**Success criteria (observable):**
- Пользователь загружает PDF и получает стабильные чанки/метаданные; повторный запуск даёт идентичные результаты для тех же входных данных.
- Пользователь выполняет инкрементальную загрузку и видит, что переобрабатываются только новые/изменённые документы.
- Оператор переключает активную версию индекса и видит, что выдача соответствует выбранной версии.
- При push в репозиторий автоматически запускаются тесты и линт, и результат виден в CI.

### Phase 2 — Graph Extraction & Postgres Graph Index
**Scope:** GRPH-01, GRPH-02, GRPH-03
**Success criteria (observable):**
- Пользователь/оператор видит извлечённые сущности и связи для новых документов в графе.
- Дубликаты сущностей объединяются; запрос по каноническому имени возвращает единый узел.
- Графовые запросы (траверсы) в Postgres возвращают ожидаемые связи для тестового набора.

### Phase 3 — Hybrid Retrieval, Caching & UI
**Scope:** RET-01, RET-02, RET-03, RET-04, CCH-01, CCH-02, UI-01, UI-02, UI-03
**Success criteria (observable):**
- Пользователь задаёт вопрос и получает ответ с цитатами на исходные чанки.
- Пользователь переключает локальный/глобальный режим и видит различие в подобранном контексте.
- Повторный запрос отдаётся быстрее за счёт кэширования.
- UI показывает выбранный режим и цитаты в ответе.

### Phase 4 — Observability & Evaluation
**Scope:** OBS-01, OBS-02, OBS-03
**Success criteria (observable):**
- Оператор видит трассировку запроса в Langfuse со стадиями retrieval и generation.
- Оператор запускает оценку RAGAS и получает метрики качества по набору запросов.
- Оператор видит базовые метрики (p95 latency, errors, throughput) для сервиса.

### Phase 5 — Security & Resilience
**Scope:** SEC-01, SEC-02, RES-01, RES-02
**Success criteria (observable):**
- Пользователь без доступа не может получить документы/узлы; авторизованный получает доступ к разрешённым данным.
- При деградации ingestion система продолжает обслуживать запросы пользователей.
- Повторы ingestion при сбоях выполняют retry/backoff без потери состояния.

## Coverage
- v1 requirements: 25
- Mapped to phases: 25
- Unmapped: 0

---
*Roadmap created: 2026-03-04*
